import 'package:get/get.dart';

/// Binding for the about module.  Currently no dependencies are
/// required, but this class exists to allow easy extension in the
/// future without modifying route definitions.
class AboutBinding extends Bindings {
  @override
  void dependencies() {
    // No controller for the about page yet
  }
}